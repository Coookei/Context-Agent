# Context Agent

A full-stack React app with a Node.js/Express server that answers questions using a Markdown-based knowledge source, with responses generated by an external LLM.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun dev
```
